apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
  namespace: default
  labels:
    prometheus-monitored: "true"
spec:
  type: NodePort
  selector:
    app: scaletestapp
  ports:
    - name: "web"
      protocol: TCP
      port: 8080
      targetPort: 8080